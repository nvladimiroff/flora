when:
  - event: push
  - event: pull_request

matrix:
  RUBY_VERSION:
    - 4.0
    - 3.4
    - 3.3
    - 3.2

steps:
    - name: build
      image: ruby:${RUBY_VERSION}-alpine
      commands:
        - apk add alpine-sdk
        - bundle install
        - rake
